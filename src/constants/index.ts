export enum USER_ROLES {
  FARMER = 'farmer',
}

export enum ORDER_STATUS {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum STORE_TYPES {
  FERTILIZER = 'fertilizer',
  SEED = 'seed',
}
